#This file is used to configure the Gradle daemon JVM (Gradle 8.8+).
#Daemon runs on JDK 21 because Gradle 8.14.5's bundled Kotlin compiler
#cannot parse JDK 25's version string. The build's Java toolchain
#(see build.gradle.kts) still compiles and tests against JDK 25.
toolchainVersion=21
